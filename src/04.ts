let nome = "João"; // Inferencia para o tipo string
// nome = 100; --- Não posso fazer isso, pois a variável nome é do tipo string.

let idade = 20; // Inferencia para o tipo number
// idade = "20"; --- Não posso fazer isso, pois a variável idade é do tipo number.

let curso = "Engenharia"; // Inferencia para o tipo string
// curso = 100; --- Não posso fazer isso, pois a variável curso é do tipo string.

console.log(nome);
console.log(idade);
console.log(curso);